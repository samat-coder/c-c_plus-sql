#include <stdio.h>
int main()
{

    int a;

    printf("enter the value");
    scanf("%d", &a);

    if (a = 0)
    {
        printf("zero value");
    }
    else if (a > 0)
    {
        printf("positive value");
    }
    else
    {
        printf("negative value");
    }
}